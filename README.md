# stolen-items-scarpet
A little script that I didn't got to work to detect if an special item was stolen by another player using scarpet api from carpet mod. I recommend to use discarpet or logger mod instead.
